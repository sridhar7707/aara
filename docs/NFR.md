# TradeGenius AI --- Non-Functional Requirements

Last measured: 2026-09-18 10:42
Generated by: `python tests/measure_performance.py --update`

## Database Query Latency (SQLite, local trades.db)

| Query | Avg (ms) |
|-------|----------|
| count_trades | 0.9 |
| latest_10_trades | 0.1 |
| open_positions | 0.0 |
| portfolio_snapshot | 0.0 |
| recommendations_14d | 1.1 |

All queries run against the local `trades.db` (no network). Target: < 50 ms per query.

## Dashboard Render Latency

| Component | Avg (ms) |
|-----------|----------|
| render_portfolio_health_hero | 2010.1 |
| render_portfolio_performance | 22.0 |
| render_positions | 41.4 |

Render functions run synchronously inside Gradio's 60-second timer.
Target: each render function < 2000 ms (worst case, cold cache).

## Memory Usage

| Metric | Value |
|--------|-------|
| Dashboard stack heap (tracemalloc) | 81943.7 KB |
| Process RSS (full bot + dashboard) | 338.9 MB |

Target: process RSS < 512 MB on HuggingFace free-tier Space (2 CPU, 16 GB RAM).

## Test Suite Performance

| Metric | Value |
|--------|-------|
| Total test time | 815.8 s |
| Result | 4 failed, 4485 passed, 49 skipped, 2718 warnings in 806.86s (0:13:26) |

Target: full test suite < 120 s was set when the suite was ~535 tests (`tests/` only, pre-Sentinel
Engine). At 4,538 tests repo-wide, 815.8s (~14 min) is the honest current baseline, not a regression —
see `TECHNICAL_DEBT.md` TD-008. The 4 known failures are an in-progress, uncommitted Performance
Learning view refactor (`applications/trading_intelligence/ui/tests/test_performance_learning_*.py`),
not a suite-wide regression — see `TECHNICAL_DEBT.md` TD-014.

## Dashboard Refresh Cycle

| Parameter | Value |
|-----------|-------|
| Gradio Timer interval | 60 s |
| DB cache TTL | 55 s (module-level `_CACHE`) |
| News feed cache TTL | 1800 s (30 min) |
| Benchmark price cache TTL | 3600 s (1 hr) |
| yfinance per-stock cache TTL | 3600 s (1 hr) |

## Availability Targets

| Component | Target | Notes |
|-----------|--------|-------|
| Trading bot (GitHub Actions) | 99% of market-hours cycles | Misses < 1 cycle / week acceptable |
| Dashboard (HuggingFace Spaces) | 95% uptime | HF free-tier may sleep; restarts in < 30 s |
| DB sync to HF | ≤ 15 min lag | `push_db()` runs each cycle |
