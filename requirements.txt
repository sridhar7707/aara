alpaca-py>=0.40.0
aiohttp>=3.9.0,<3.10.0
# ML library pins — must match the training environment.
# After changing any of these, trigger retrain.yml to rebuild models.
# Versions verified against ubuntu-latest GitHub Actions runner 2026-06-25.
torch>=2.12.0,<3.0
scikit-learn>=1.9.0,<2.0
xgboost>=3.0.0,<4.0
ta>=0.11.0
huggingface-hub>=0.20.0
# Gradio: CI and the HF Space run Python >=3.10 and use gradio 5.x. Local
# Python 3.9 dev can't install gradio>=5.0.0 (Requires-Python >=3.10), so it
# falls back to gradio==4.44.1; that combo needs pydantic pinned to 2.10.6
# to avoid a Python-3.9-only schema-generation regression in pydantic>=2.11
# (AttributeError: '_SpecialForm' object has no attribute 'replace').
gradio>=5.0.0,<6.0.0; python_version >= "3.10"
gradio==4.44.1; python_version < "3.10"
pydantic==2.10.6; python_version < "3.10"
plotly>=5.0.0
python-telegram-bot>=20.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
yfinance>=0.2.0
schedule>=1.2.0
sqlalchemy>=2.0.0
loguru>=0.7.0
python-dotenv>=1.0.0
requests>=2.31.0
matplotlib>=3.7.0
seaborn>=0.12.0
joblib>=1.3.0

# Phase 7 — FinBERT sentiment
transformers>=4.35.0

# Phase 9 — FRED macro signals
fredapi>=0.5.0

# Phase 11 — Reddit/WSB sentiment
praw>=7.7.0

# Phase 7 — NewsAPI headlines
newsapi-python>=0.2.7

# Phase 12 — DuckDB analytics layer
duckdb>=1.0.0

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0

# Sentinel brand governance validator (tools/validators/) -- was only ever
# present transitively via other packages; declared explicitly since our
# own code imports it directly.
PyYAML>=6.0.0

# HF ZeroGPU runtime requirement for applications/trading_intelligence/main.py's
# _zero_gpu_startup_probe(). CI's "Run Trading Intelligence tests" step
# imports main.py (via test_main.py), so this must be installable here too,
# not just in requirements_trading_intelligence.txt. Verified via a clean pip
# dependency-resolution dry-run under both Python 3.9 (gradio==4.44.1) and
# Python 3.11 (gradio>=5.0.0,<6.0.0, resolves to 5.50.0) -- no conflicts.
spaces==0.46.0
